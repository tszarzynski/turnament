import { page } from '@vitest/browser/context';
import { render } from 'vitest-browser-react';
import {{pascalCase name}} from './{{pascalCase name}}';

describe('{{pascalCase name}}', () => {
   it('renders', async () => {
      render(<{{pascalCase name}} />);
      // TODO: assert meaningful behavior
   });
});
